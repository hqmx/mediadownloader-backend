# 환경 설정 예제 파일
# 이 파일을 .env로 복사하고 실제 값들로 수정하세요

# 실행 환경 (development, production, test)
NODE_ENV=production

# 서버 포트
PORT=3000

# yt-dlp 바이너리 경로 (EC2: /usr/local/bin/yt-dlp, 로컬: yt-dlp)
YTDLP_PATH=/usr/local/bin/yt-dlp

# 임시 다운로드 디렉토리
TEMP_DIR=/tmp/mediadownloader

# CORS 허용 도메인 (프로덕션에서는 실제 도메인으로 설정)
# CORS_ORIGIN=https://yourdomain.com

# 로깅 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# 최대 파일 크기 (바이트)
MAX_FILE_SIZE=1073741824

# 동시 다운로드 제한
MAX_CONCURRENT_DOWNLOADS=5

# ===========================================
# SmartProxy 설정 (Plan B 스텔스 시스템)
# ===========================================

# SmartProxy 활성화 여부
SMARTPROXY_ENABLED=true

# SmartProxy 주거용 프록시 정보 (대시보드에서 복사)
SMARTPROXY_HOST=rotating-residential.smartproxy.com
SMARTPROXY_PORT=101
SMARTPROXY_USERNAME=user-session-xxxxxx-country-worldwide
SMARTPROXY_PASSWORD=SmP_xxxxxxxxxx

# 세션 관리 (밀리초 단위)
SMARTPROXY_SESSION_DURATION=600000
SMARTPROXY_STICKY_SESSION=false

# 국가 설정 (us, uk, de, worldwide 등)
SMARTPROXY_COUNTRY=worldwide
SMARTPROXY_RETRY_ATTEMPTS=3

# Plan B 스텔스 레벨 (BASIC, MEDIUM, MAXIMUM, EXTREME)
STEALTH_LEVEL=MAXIMUM

# ===========================================
# 스텔스 브라우저 설정
# ===========================================

# Playwright 브라우저 폴백 사용
USE_BROWSER_FALLBACK=true

# 헤드리스 모드 (true: 보이지 않음, false: 브라우저 창 보임)
BROWSER_HEADLESS=true

# 인간 행동 시뮬레이션 활성화
SIMULATE_HUMAN=true

# 쿠키 저장 및 재사용
SAVE_COOKIES=true

# ===========================================
# 디버깅 및 로깅
# ===========================================

# 디버그 모드 (true: 브라우저 창 보임, 상세 로깅)
DEBUG_MODE=false

# 상세 로깅 활성화
VERBOSE_LOGGING=false

# 성공/실패 통계 수집
COLLECT_STATS=true